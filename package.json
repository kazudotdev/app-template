{
  "private": true,
  "name": "app-template",
  "version": "1.0.0",

  "scripts": {
    "db": "bun run --cwd packages/db",
    "mobile": "bun run --cwd apps/mobile",
    "backend": "bun run --cwd apps/backend"
  },
  "engines": {
    "pnpm": "please_use_bun_instead",
    "npm": "please_use_bun_instead"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "packageManager": "bun",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
