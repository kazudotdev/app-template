{
  "private": true,
  "name": "@db",
  "version": "1.0.0",
  "scripts": {
    "generate": "bun run generate:local && bun run generate:remote",
    "generate:local": "bunx drizzle-kit generate:sqlite --config ./drizzle.config.local.ts && bun run build:local",
    "build:local": "bun build ./drizzle/local/migrations.js --outfile ./migrations_local.js",
    "generate:remote": "bunx drizzle-kit generate:sqlite --config ./drizzle.config.remote.ts && bun run build:remote",
    "build:remote": "bun build ./drizzle/remote/migrations.js --outfile ./migrations_remote.js",
    "migrate": "bun run migration.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "drizzle-kit": "0.20.13"
  },
  "dependencies": {
    "@libsql/client": "0.4.2",
    "@paralleldrive/cuid2": "^2.2.2",
    "drizzle-orm": "0.29.3",
    "expo-sqlite": "^13.2.2",
    "text-encoding": "^0.7.0"
  }
}
