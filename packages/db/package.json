{
  "private": true,
  "name": "@db",
  "version": "1.0.0",
  "scripts": {
    "generate": "bunx drizzle-kit generate:sqlite && bun run build",
    "build": "bun build ./drizzle/migrations.js --outfile ./migrations.js",
    "migrate": "bun run migration.ts"
  },
  "license": "ISC",
  "devDependencies": {
    "drizzle-kit": "0.20.13"
  },
  "dependencies": {
    "@libsql/client": "0.4.2",
    "@paralleldrive/cuid2": "^2.2.2",
    "drizzle-orm": "0.29.3",
    "expo-sqlite": "^13.2.2",
    "text-encoding": "^0.7.0"
  }
}
